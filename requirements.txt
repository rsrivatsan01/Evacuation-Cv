# ─────────────────────────────────────────────
# AI Evacuation Optimization System
# requirements.txt  |  CPU-only (laptop inference)
# Python 3.13  |  Windows
# ─────────────────────────────────────────────

# ── Core ML / Detection ──────────────────────
# NOTE: Install PyTorch CPU-only FIRST (separately) via:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

# YOLOv8 + ByteTrack (via supervision)
ultralytics==8.3.40

# Supervision: ByteTrack wrapper + annotation utilities
supervision==0.24.0

# ── Video & Image Processing ─────────────────
opencv-python==4.10.0.84
Pillow==11.1.0

# ── Data Processing ───────────────────────────
numpy==2.1.3
pandas==2.2.3
scikit-learn==1.6.1

# ── Path Planning ─────────────────────────────
networkx==3.4.2

# ── Dashboard ────────────────────────────────
flask==3.1.0
flask-cors==5.0.0

# ── Visualization ─────────────────────────────
matplotlib==3.10.0

# ── Utilities ────────────────────────────────
pyyaml==6.0.2
tqdm==4.67.1
requests==2.32.3
python-dotenv==1.0.1